# Sportsbook UI

Reserved category for Oddium betting presentation/state helpers.

V38 rules:
- one private navigation page per user;
- SIMPLE and COMBINÉ use a match carousel;
- every match card shows the home logo left and away logo right;
- combo ticket is text-only and is the single allowed extra ephemeral page;
- combo ticket owns only `Vider` and `Valider` and edits itself in place.
